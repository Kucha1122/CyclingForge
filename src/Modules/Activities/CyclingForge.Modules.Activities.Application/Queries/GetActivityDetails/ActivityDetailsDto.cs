namespace CyclingForge.Modules.Activities.Application.Queries.GetActivityDetails;

public sealed record ActivityDetailsDto(
    Guid Id,
    long StravaActivityId,
    string Name,
    string Type,
    DateTime StartDate,
    float DistanceKm,
    float DistanceMeters,
    TimeSpan MovingTime,
    TimeSpan ElapsedTime,
    float TotalElevationGain,
    float? AverageSpeed,
    float? MaxSpeed,
    float? AverageHeartRate,
    float? MaxHeartRate,
    float? AveragePower,
    float? MaxPower,
    float? NormalizedPower,
    float? IntensityFactor,
    float? TrainingStressScore,
    float? VariabilityIndex,
    int? FtpUsed,
    float? Best5MinPower,
    float? Best20MinPower,
    float? Best60MinPower,
    int? EstimatedFtpFromActivity,
    bool? DeviceWatts,
    DateTime SyncedAt);
